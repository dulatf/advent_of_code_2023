(ns advent-of-code-2023.core
  (:gen-class))

(comment
  (def f (fn [x y]
           (+ 2 x y)))
  (f 2 3)
  )

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
